import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  chart: {
    borderRadius: 20,
    marginBottom: 30
  },
  bemVindoContainer: {
    height: 50,
    width: '90%',
    justifyContent: 'center',
  },
  bemVindoText: {
    fontSize: 18,
    color: "green"
  }
});